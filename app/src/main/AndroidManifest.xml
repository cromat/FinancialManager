<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mat.financialmanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.invoice.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.invoice.AddEditInvoiceActivity"
            android:label="@string/title_activity_add_invoice"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.invoice.InvoiceDetailsActivity"
            android:label="@string/title_activity_invoice_details"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.fund.AddEditFundActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.share.AddEditShareActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".activity.fund.FundDetailsActivity"
            android:label="@string/title_activity_fund_details"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.fund.FundListActivity"
            android:label="@string/title_activity_fund_list"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.share.ShareListActivity"
            android:label="@string/title_activity_share_list"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.share.ShareDetailsActivity"
            android:label="@string/title_activity_share_details"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>